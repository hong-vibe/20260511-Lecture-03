# GitHub 저장소 검색 기능 구현 프롬프트 기록

이 문서는 AI와 함께 GitHub REST API를 활용한 저장소 검색 기능을 구현하며 진행했던 대화와 요청 사항(프롬프트)들을 시간순으로 정리한 기록입니다.

## 1단계: API URL 설계 및 기본 Fetch 예시 요청
**요청 내용:**
GitHub REST API에서 저장소를 검색하는 요청 URL을 만들고 싶습니다.
조건:
1. 검색어는 사용자가 입력한 keyword를 사용합니다.
2. 저장소 검색 API를 사용합니다.
3. stars 기준으로 정렬합니다.
4. 내림차순으로 정렬합니다.
5. 결과는 6개만 가져옵니다.
6. fetch에서 사용할 수 있는 URL 문자열 예시를 보여주세요.

**진행 결과:**
조건에 맞는 GitHub Search API(`https://api.github.com/search/repositories`) URL의 구조와 간단한 `fetch` 사용 예제를 제공받았습니다.

---

## 2단계: 템플릿 리터럴 적용 및 초보자용 주석 추가
**요청 내용:**
URL을 JavaScript 함수 안에서 사용할 수 있도록 template literal 방식으로 다시 작성해 주세요.
조건:
1. 검색어 변수 이름은 keyword로 고정해 주세요.
2. 검색어는 encodeURIComponent로 처리해 주세요.
3. 결과 개수는 6개로 제한해 주세요.
4. 초보자가 이해할 수 있도록 각 부분에 주석을 달아 주세요.

**진행 결과:**
`searchGithubRepositories(keyword)` 라는 함수 형태로 코드를 고도화하였으며, 각 줄의 의미(API 엔드포인트, 정렬 옵션, 인코딩의 필요성 등)를 초보자도 쉽게 이해할 수 있도록 상세한 주석을 달았습니다.

---

## 3단계: 파일 생성
**요청 내용:**
파일로 만드세요

**진행 결과:**
작성된 JavaScript 코드를 워크스페이스 내 `github-search.js` 파일로 저장했습니다.

---

## 4단계: 화면 UI (HTML) 구현 및 연동
**요청 내용:**
확인해볼 수 있는 index.html 화면을 만들어주세요

**진행 결과:**
검색 결과를 직접 눈으로 확인할 수 있도록 `index.html`을 생성했습니다.
- 입력창과 검색 버튼, 예쁜 카드 형태의 결과를 렌더링하기 위한 CSS가 포함되었습니다.
- 기존 `github-search.js`의 `console.log` 방식을 수정하여, DOM(`document.getElementById('results')`)을 조작해 화면에 결과를 뿌려주고 검색 버튼과 연동되도록 업데이트했습니다.

---

## 5단계: 프롬프트 진행 기록 저장
**요청 내용:**
프롬프트 진행 내용을 prompt.md로 만들어 저장해주세요

**진행 결과:**
현재 보고 계시는 `prompt.md` 파일에 전체 개발 진행 과정을 기록하여 저장했습니다.

---

## 6단계: HTML/CSS/JS 파일 구조 분리
**요청 내용:**
순수 HTML/CSS/JavaScript로 GitHub Repository Search Board 앱의 기본 HTML 구조를 작성해 주세요. (조건: 검색창, 검색 버튼, 상태 메시지, 카드 목록 영역, style.css/app.js 분리)

**진행 결과:**
기존 코드를 `index.html` (구조), `style.css` (디자인), `app.js` (동작) 3개의 파일로 명확하게 분리하여 유지보수성을 크게 높였습니다.

---

## 7단계: 시각적 상태 섹션(Status Section) 추가
**요청 내용:**
검색창 아래 섹션에 상태 섹션을 만들어주세요.

**진행 결과:**
단순 텍스트였던 상태 메시지를 시각적으로 돋보이는 `<section id="statusSection">` 박스로 만들고, 상태(검색 전, 진행 중, 완료, 결과 없음, 에러)에 맞춰 아이콘(💡, ⏳, ✅, 🤔, 🚨)과 배경색이 변하도록 업데이트했습니다.

---

## 8단계: 빈 검색어 예외 처리 개선
**요청 내용:**
검색어창에 빈칸인 상태로 엔터를 치면 상태 메시지 섹션에만 반응이 나와야하는데 팝업창도 뜹니다. 팝업창은 안뜨게 하세요

**진행 결과:**
빈 검색어 입력 시 동작하던 `alert()` 팝업을 제거하고, 대신 상태 섹션이 경고 모드(⚠️)로 변하면서 "검색어를 입력해주세요."라는 메시지를 띄우도록 `app.js`를 수정했습니다.

---

## 9단계: HTML/JS 구조 단순화 및 ID 명칭 통일
**요청 내용:**
결과 목록 영역의 id를 `repoList`로, 상태 메시지 영역의 id를 `statusMessage`로 고정하고, 입력창은 `searchInput`, 버튼은 `searchButton`으로 변경해 주세요. (JavaScript에서 선택하기 쉽게 구조 단순화)

**진행 결과:**
기존에 나뉘어 있던 상태 아이콘과 섹션을 `<div id="statusMessage">` 하나로 통합하여 HTML 구조를 단순화했습니다. 또한 버튼의 ID를 `searchButton`으로 변경하고, JS 코드도 하나의 DOM 요소로만 상태(메시지 및 이모지)를 제어하도록 전면 수정했습니다.

---

## 10단계: 다크 모드 기반 반응형 CSS 디자인 적용
**요청 내용:**
흰색 배경의 Bootstrap 느낌을 버리고 다크 모드 기반의 검색 보드로 CSS를 전면 재작성해 주세요. 검색창과 버튼은 상단에 배치하고, 그리드 레이아웃을 통해 모바일(1열), 태블릿(2열), 데스크톱(3열)에 반응하도록 만들어 주세요. 또한 초보자를 위해 상세한 주석을 달아주세요.

**진행 결과:**
`style.css`를 다크 테마(#121212 배경)로 전면 수정했습니다. `@media` 쿼리를 활용해 화면 너비에 따라 1~3열로 카드가 자동 정렬되도록 반응형 그리드를 구현했으며, 마우스 호버(Hover) 시 카드가 떠오르는 애니메이션과 네온 느낌의 포커스 효과를 더했습니다. 모든 CSS 속성 윗줄에 그 역할이 무엇인지 알 수 있게 친절한 주석을 추가했습니다.

---

## 11단계: 검색 버튼 클릭 이벤트 로직 보완 및 주석 강화
**요청 내용:**
순수 JavaScript로 검색 버튼 클릭 시 입력창의 값을 읽는 코드를 작성해 주세요. (조건: 앞뒤 공백 제거, 빈칸일 시 에러 메시지 표시, 값이 있으면 console.log 출력, 초보자용 주석 추가)

**진행 결과:**
`app.js`의 버튼 클릭 이벤트 리스너(`addEventListener`) 부분을 요구사항에 맞춰 수정했습니다. `trim()`을 사용한 공백 제거, 빈 문자열(`""`) 검사 로직을 초보자가 이해하기 쉽도록 번호를 매긴 상세한 한글 주석과 함께 작성했으며, 정상 입력 시 `console.log("검색어:", keyword)`로 값을 출력하도록 기능을 추가했습니다.

---

## 12단계: 검색 로직 통합 및 엔터 키 지원
**요청 내용:**
Enter 키를 눌렀을 때도 검색이 실행되도록 코드를 보완해 주세요. (조건: 기존 버튼 클릭 기능 유지, handleSearch 함수 분리, 초보자용 주석 추가)

**진행 결과:**
중복되는 검색 로직을 `handleSearch`라는 별도의 함수로 분리하여 코드의 효율성을 높였습니다. 이제 검색 버튼을 클릭하거나 입력창에서 `Enter` 키를 눌렀을 때 모두 이 함수가 실행됩니다. 초보자도 이해하기 쉽도록 함수 분리의 이유와 이벤트 연결 방식을 주석으로 상세히 설명했습니다.

---

## 13단계: async/await 기반 fetchRepos 함수 작성 및 적용
**요청 내용:**
GitHub Repository Search API를 호출하는 `fetchRepos` 함수를 작성해 주세요. (조건: keyword 매개변수, async/await 사용, encodeURIComponent 처리, stars 내림차순/6개 제한, response.ok 확인 및 에러 처리, JSON 반환, 초보자용 주석 추가)

**진행 결과:**
데이터 요청 로직을 별도의 비동기 함수인 `fetchRepos`로 분리했습니다. `fetch`와 `async/await` 문법을 사용하여 가독성을 높였으며, `response.ok` 체크와 `throw new Error`를 통해 예외 상황을 견고하게 처리하도록 구현했습니다. 기존의 `searchRepositories` 함수도 이 `fetchRepos`를 호출하여 데이터를 비동기적으로 받아오도록 리팩토링했습니다.

---

## 14단계: 저장소 카드 표시 정보 확장 (언어, 포크 등)
**요청 내용:**
저장소 카드에 이름, 설명, 언어, Star 수, Fork 수, 링크를 표시할 수 있도록 JSON 필드를 정리하고 코드를 수정해 주세요. 데이터 부재 시의 기본 문구도 제안해 주세요.

**진행 결과:**
GitHub API 응답 데이터 중 필요한 필드(`language`, `forks_count` 등)를 식별하여 표로 정리했습니다. 이를 바탕으로 `app.js`의 `renderCards` 함수를 수정하여 언어와 포크 정보를 추가로 표시하고, 데이터가 없는 경우를 대비해 "설명이 없습니다.", "알 수 없음" 등의 기본값이 출력되도록 보완했습니다. 또한 `style.css`에 정보 태그들의 시각적 스타일을 추가했습니다.

---

## 15단계: 프로젝트 README.md 작성 및 문서화
**요청 내용:**
현재 프로젝트 내용을 바탕으로 프로젝트 명칭, 기술 스택, 주요 기능, 배운 점 등을 포함한 `README.md`를 작성해 주세요. (조건: "AI 최종 판단은 개발자" 메시지 포함, 초보자용 가이드 스타일)

**진행 결과:**
프로젝트의 전체적인 개요와 실행 방법, 학습 성과를 담은 `README.md` 완성본을 작성했습니다. AI와의 협업 과정과 검토 기준을 명시하여 프로젝트의 신뢰도를 높였으며, 향후 개선 사항까지 포함하여 프로젝트의 발전 방향을 제시했습니다. 최종적으로 모든 변경 사항을 GitHub에 동기화했습니다.

---

## 16단계: handleSearch 로직 통합 및 함수명 변경 (renderRepos)
**요청 내용:**
`handleSearch` 함수에서 `fetchRepos`로 가져온 데이터의 `items` 배열을 `renderRepos` 함수에 전달하도록 코드를 수정해 주세요. (조건: `const data = await fetchRepos(keyword)` 사용, `renderRepos(data.items)` 호출, 기존 입력값 검증 유지 등)

**진행 결과:**
중간 매개 역할을 하던 `searchRepositories` 함수를 제거하고, `handleSearch` 함수가 직접 데이터 요청(`fetchRepos`)과 화면 렌더링(`renderRepos`)을 제어하도록 구조를 통합했습니다. 요청에 따라 `renderCards`의 이름을 `renderRepos`로 변경하였으며, `async/await`를 활용해 비동기 흐름을 한곳에서 관리하도록 리팩토링했습니다.

---

## 17단계: 검색 실행 흐름 고도화 (로딩/결과없음/에러 처리 및 버튼 제어)
**요청 내용:**
검색 실행 흐름에 `try/catch/finally`를 적용하고, 로딩 상태("검색 중입니다..."), 결과 없음, 에러 처리 메시지를 보완해 주세요. 또한 검색 중에는 버튼을 비활성화하고 이전 결과를 비우도록 기능을 추가해 주세요.

**진행 결과:**
`handleSearch` 함수에 `finally` 블록을 추가하여 검색 완료 후 버튼 활성화 상태를 보장하도록 했습니다. 검색 중 버튼 비활성화(`disabled = true`)와 결과 목록 초기화 로직을 추가하여 중복 요청을 방지하고 UI 일관성을 높였습니다. 상황별(로딩, 결과 없음, 에러)로 더욱 명확한 한국어 안내 메시지를 출력하도록 수정하고 초보자용 주석을 보강했습니다.

---

## 18단계: 프로젝트 최종 README.md 업데이트 및 최적화
**요청 내용:**
최신 프로젝트 내용을 반영하여 `README.md`를 최종적으로 작성해 주세요. (조건: 11가지 필수 항목 포함, AI 판단 메시지 포함 등)

**진행 결과:**
리팩토링된 `handleSearch` 로직과 새롭게 추가된 상태 처리 기능(버튼 제어, 로딩 메시지 등)을 반영하여 `README.md`를 최신화했습니다. 프로젝트의 목적부터 학습 목표, 기술 스택, 그리고 AI와의 협업 경험까지 상세히 기술하여 완성도 높은 프로젝트 문서를 구축했습니다. 모든 작업 내역을 GitHub에 최종 동기화했습니다.

---

## 19단계: 카드 설명 즉시 번역 기능 구현 (MyMemory API)
**요청 내용:**
검색된 카드 안의 영문 설명을 한글로 즉시 번역하는 기능을 추가해 주세요. (조건: AI API 미사용, 브라우저 탐지 후 지원 브라우저에서만 버튼 노출, 비지원 브라우저에서는 버튼 숨김, 버튼 클릭 시 확인 없이 즉시 번역)

**진행 결과:**
무료 번역 서비스인 MyMemory API를 활용하여 카드 설명을 즉시 한글로 번역하는 기능을 구현했습니다. `isTranslationSupported()` 함수로 Chromium 기반 브라우저(크롬, 엣지)를 탐지하여 버튼 노출을 제어하고, `translateText()` 함수로 각 카드의 영문 설명을 번역합니다. 번역/원문 토글이 가능하며, 원문은 `data-original` 속성에 저장하여 복원할 수 있습니다.

---

## 20단계: 번역 API 한도 확장 및 캐싱 시스템 구축
**요청 내용:**
번역 API의 일일 무료 한도(1,000자) 문제를 해결하기 위한 대책을 마련해 주세요.

**진행 결과:**
두 가지 최적화 작업을 수행했습니다. 첫째, `localStorage`를 활용한 **번역 캐싱 시스템**을 도입하여 동일한 텍스트에 대한 중복 API 호출을 원천 차단했습니다. 둘째, API 요청 시 이메일 파라미터(`de`)를 추가하여 일일 무료 한도를 5,000자에서 **50,000자**로 10배 확장했습니다. 이를 통해 서비스 안정성과 효율성을 동시에 확보했습니다.

---

## 21단계: 새 검색 시 번역 버튼 상태 초기화 오류 수정
**요청 내용:**
첫 번째 검색 후 번역 버튼이 "원문 보기"인 상태에서 두 번째 검색을 하면 버튼이 초기화되지 않는 문제를 해결해 주세요.

**진행 결과:**
`handleSearch` 함수 내에 번역 상태 초기화 로직을 추가했습니다. 새로운 검색이 시작될 때마다 `isTranslated` 변수를 `false`로 리셋하고, 버튼 텍스트를 "🌐 번역"으로 돌려놓으며 활성화 스타일(`active` 클래스)을 제거하도록 수정했습니다. 이로써 새로운 영문 검색 결과와 버튼의 상태가 항상 일치하게 되었습니다.

---

## 22단계: 저장소 설명 5줄 제한 및 레이아웃 최적화
**요청 내용:**
설명이 너무 길어서 레이아웃이 깨지는 문제를 해결하기 위해, 설명 부분을 최대 5줄로 제한해 주세요.

**진행 결과:**
CSS의 `line-clamp` 속성을 활용하여 저장소 설명(`.description`)이 최대 5줄까지만 표시되도록 제한했습니다. 5줄을 초과하는 텍스트는 자동으로 말줄임표(...)가 표시되도록 설정하여, 검색 결과 그리드의 높이가 일정하게 유지되고 시각적으로 훨씬 깔끔한 대시보드 레이아웃을 완성했습니다.

---

## 23단계: GitHub 주간 인기 트렌드 TOP 10 및 순위 그래프 구현
**요청 내용:**
웹앱 하단에 최근 일주일간 인기 있는 GitHub 저장소 TOP 10 리스트와 별점 순위 그래프를 추가해 주세요. (조건: Tailwind CSS 미사용, 기존 Vanilla CSS 스타일 유지, Chart.js 활용)

**진행 결과:**
하단에 새로운 `trending-section`을 설계하고 구현했습니다. 최근 7일 내 생성된 저장소 중 별점이 가장 높은 상위 10개를 실시간으로 API 호출하여 좌측에는 **순위 배지 리스트**, 우측에는 **Chart.js 기반의 가로 막대 그래프**로 시각화했습니다. 각 배지 클릭 시 해당 저장소의 상세 정보를 즉시 검색하는 편의 기능도 추가했으며, 기존 다크 모드 테마와 완벽하게 조화를 이루도록 스타일링했습니다.

---

## 24단계: 트렌드 섹션 Bump Chart 방식으로 전면 개선
**요청 내용:**
Gemini AI가 제안한 순위 변동 차트(Bump Chart) 방식으로 트렌드 섹션을 교체해 주세요. X축은 '1주 전'/'현재', Y축은 1위가 맨 위인 역순, 데이터는 인사이트 있는 가상 키워드 순위 데이터를 사용해 주세요.

**진행 결과:**
기존 API 호출 기반 bar 차트를 `TREND_DATA` 하드코딩 방식의 Bump Chart(라인 차트)로 전면 교체했습니다. `AI/LLM 5위→1위 급상승`, `Rust 7위→3위 급상승`, `Python 1위→4위 하락` 등 실제 업계 흐름을 반영한 스토리텔링 데이터를 설계했습니다. 배지에는 ▲/▼ 상승·하락 인디케이터를 추가하고, 상위권(1~3위) 라인은 굵게 강조하여 시각적 인사이트를 극대화했습니다. 배지 클릭 시 해당 키워드를 즉시 검색하는 편의 기능도 유지했습니다.

---

## 25단계: 전체 UI 디자인 테마 변경 (다크 모드 → 미색 라이트 모드)
**요청 내용:**
현재 UI 디자인과 색감이 너무 화려해서 눈이 아프니, 첨부한 사진(B안: 쿨 라이트 그레이 + 다크 네이비 포인트)과 같이 눈이 편안한 미색 계열 디자인으로 변경해 주세요.

**진행 결과:**
기존 다크 모드(`121212` 배경)에서 눈이 편안한 쿨 라이트 그레이(`#EEF2F7`) 테마로 전체 CSS 팔레트를 전면 교체했습니다. 카드와 입력창 등 요소는 화이트 배경에 은은한 그림자(box-shadow)를 추가해 깊이감을 주고, 텍스트와 포인트 색상은 다크 네이비(`#1A2332`) 및 차분한 파스텔톤으로 통일했습니다. `app.js` 내 트렌드 차트의 선/점 색상 역시 라이트 테마에 어울리는 전문적인 컬러로 수정하여 전체적인 시각적 안정감을 대폭 향상시켰습니다.

---

## 26단계: 트렌드 차트 UI 고도화 및 클릭 이벤트 디버깅
**요청 내용:**
1. 차트 제목들을 중앙 정렬할 것.
2. 왼쪽 배지 리스트를 제거하고 4주간 그래프가 1열 꽉 차게 변경할 것.
3. 그래프의 왼쪽, 오른쪽에 커스텀 레이블 텍스트("1위 AI / LLM ▲ 7" 형태)를 표시할 것.
4. 차트 내 여러 점이 동시에 툴팁에 표시되는 버그 수정.
5. 중간 선이나 점이 아닌, 패딩 바깥쪽 텍스트 레이블만 클릭해야 검색이 작동하도록 수정할 것.

**진행 결과:**
- `app.js`에 `startLabelPlugin`과 `endLabelPlugin`을 추가하여 캔버스 좌우에 키워드 이름과 변동 사항을 직접 그려주는 레이블 기능을 구현했습니다.
- Chart.js의 기본 `interaction.axis`를 `xy`로 변경하여 툴팁 중복 표시 오류를 해결했습니다.
- `onClick` 및 `onHover` 이벤트를 Chart.js 내부 옵션에서 분리하고, 캔버스(`canvas`)에 직접 네이티브 이벤트 리스너를 달았습니다. 캔버스 가장자리(패딩) 영역에 마우스가 위치했을 때만 거리를 수학적으로 계산(`Math.abs`)하여 클릭을 인식하도록 구현함으로써, 점 클릭을 방지하고 레이블 클릭 검색 기능을 완벽하게 적용했습니다.
- `style.css`에서 차트 영역 높이를 480px로 확대하고 폰트 크기를 키워 시각적 가독성을 높였습니다.

---

## 27단계: 브랜드 로고 스타일 적용 및 다크 모드 원복
**요청 내용:**
1. 웹앱의 제목을 "GitHub Scanner"로 변경하고 눈에 잘 띄게 디자인할 것.
2. 다시 처음의 다크모드로 변경하고 제목 색상을 다크모드에 맞게 적용할 것.
3. 초기 안내 문구를 "검색어를 입력하거나 키워드를 클릭해서 검색하세요"로 변경할 것.

**진행 결과:**
- `style.css`를 전면 수정하여 배경을 다시 `#121212` 다크 모드로 되돌렸으며, `app.js`의 형광 네온 컬러(딥 오렌지, 민트 등) 데이터도 기존 다크 모드 색상으로 완벽히 복원했습니다.
- `index.html`의 브라우저 탭 타이블과 `<h1>`을 `GitHub Scanner`로 변경하고, `style.css`에서 `linear-gradient`와 `text-shadow`를 활용한 빛나는 로고 텍스트 이펙트를 적용하여 다크 모드에서 압도적인 존재감을 갖도록 디자인했습니다.
- 상태 메시지 초깃값을 "검색어를 입력하거나 키워드를 클릭해서 검색하세요"로 변경하여 사용자가 트렌드 배지 클릭 기능을 직관적으로 인지할 수 있도록 개선했습니다.
